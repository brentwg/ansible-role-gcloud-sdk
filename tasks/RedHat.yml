---
- name: Create the YUM repository (RedHat)
  yum_repository:
    name: "{{ yum_repo_name }}"
    description: "{{ yum_repo_description }}"
    baseurl: "{{ yum_repo_baseurl }}"
    gpgkey: "{{ yum_repo_yum-key }} {{ yum_repo_rpm-package-key }}"
    gpgcheck: yes
    enabled: yes
